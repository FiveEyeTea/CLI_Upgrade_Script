# CLI_Upgrade_Script
A simple Python script which runs both Winget and Chocolatey to check if there are any available updates and, if there are, it re-runs the command to automatically upgrade those packages.
